spring:
  application:
    name: image-service
server:
  port: 0
image:
  path: /Programming/Java/StreamingService/Backend/image-service/images
  default: default_image.png
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
logging:
  level:
    '[org.springframework.web]': debug