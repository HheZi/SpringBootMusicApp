spring:
  application:
    name: UserService
  datasource:
    url: jdbc:postgresql://localhost:5432/Users
    password: postgres
    username: postgres
server:
  port: 0
logging:
  level:
    '[org.springframework.web]': debug