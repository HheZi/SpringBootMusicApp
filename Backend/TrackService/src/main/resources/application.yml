spring:
  application:
    name: track-service
  datasource:
    url: jdbc:postgresql://localhost:5432/TrackDB
    password: postgres
    username: postgres
server:
  port: 0
logging:
  level:
    '[org.springframework.web]': debug