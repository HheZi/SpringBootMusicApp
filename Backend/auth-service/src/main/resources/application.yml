spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
  config:
    import: "configserver:"
  application:
    name: auth-service
  profiles:
    active: dev
