spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
  config:
    import: "optional:configserver:"
  application:
    name: auth-service
  profiles:
    active: local
