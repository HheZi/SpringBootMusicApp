spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
  config:
    import: "configserver:"
  application:
    name: audio-service
  profiles:
    active: local
